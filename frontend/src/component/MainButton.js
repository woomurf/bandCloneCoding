import React from "react";
import '../scss/component.scss';

const Button = () => {
  return (
    <div className="main_button">
      <p className="main_push">Login</p>
    </div>
  );
};

export default Button;